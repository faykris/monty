#include "monty.h"
/**
 * _queue - Sets the format of the data to a queue (FIFO).
 * @stack: Elements past for stack
 * @lines: Lines of read past bytecodes
 * Return: None.
 */
void _queue(stack_t **stack, unsigned int lines)
{

	f_struct.format = AS_QUEUE;

	stack = stack;
	lines = lines;
}
